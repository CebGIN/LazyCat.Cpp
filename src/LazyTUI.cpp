#include "LazyTUI.hpp"
#include <iostream>

void LazyTUI::run() {
    std::cout << "Starting Guayaba Engine TUI (Stub)...\n";
    std::cout << "Currently, the TUI is under construction.\n";
    std::cout << "Please configure the settings via lazycat.conf directly, or use CLI commands.\n";
    std::cout << "Returning to terminal (restoring cooked mode)...\n";
}
